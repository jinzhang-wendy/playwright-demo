{
  "name": "playwright-demo-saucedemo",
  "version": "1.0.0",
  "description": "Playwright E2E demo on saucedemo.com — login / cart / checkout core flow (Page Object pattern)",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "better-sqlite3": "^12.11.1"
  }
}
